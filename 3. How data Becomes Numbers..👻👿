How Data Becomes Numbers..

You already know how a neuron works. It takes numbers as inputs, combines them using weights and bias, applies an activation function, and produces
an output..

But real-world data is not naturally numeric.

A picture is not a number.
A word is not a number.
A sound is not a number.
A label like cat or dog is not a number..

Yet, a neuron can only handle numeric values like:

3.4
0.12

vectors, such as [0.5, 1.2, -0.7]
Converting Real-World Data to Numbers
To feed anything into a neural network, we must convert it into numerical form:

Images → pixel values like [124, 98, 210, …]
Words (e.g., "apple") → token IDs like 42
Categories (e.g., “dog”) → numeric labels like 1
Audio → amplitude values such as [0.12, 0.15, 0.08, …]

Note: Only after this conversion can a neuron perform its mathematical operations.

Goal..

By the end of this module, you will understand how to convert any kind of data, image, text, sound, or categories into the numeric vector form that 
neural networks expect.

################################3

Why Neural Networks Can Only Handle Numbers
Let’s understand this using a simple NumPy experiment.

We try the same dot product twice:
With numbers → works (neurons can multiply and add numbers)
With strings → fails (because "hello" × "coders" is not valid math)

Conclusion:
If data isn’t numeric, a neural network cannot process it.
So images, text, audio, labels, everything must be converted into numbers first.

Code Explanation

Create two numeric arrays: a and b.
Compute their dot product → NumPy returns a number.
Create a string array: c.
Try a dot product with strings.
NumPy throws an error because text cannot be multiplied.
After running the code you will see

Dot product using NUMBERS: 32
Error when using STRINGS: data type must provide an itemsize

#############################

import numpy as np

# Numeric operation works
a = np.array([1, 2, 3])
b = np.array([4, 5, 6])

print("Dot product using NUMBERS:", np.dot(a, b))  # This will work

# Strings fail
c = np.array(["hello", "coders"])

try:
    print("Dot product using STRINGS:", np.dot(c, c))  # This will fail
except Exception as e:
    print("Error when using STRINGS:", e)

####################3 

How Tabular Data Becomes Numbers..

Imagine you have a dataset with information about people:

Name	Age	Color	Country
Alice	25	Red	India
Bob	30	Blue	USA
Carol	22	Green	Japan
Age is numeric → the network can understand it.
Color and Country are words → the network cannot process them directly.
Goal: Represent these words as numbers so the neural network can work with them.

There are two simple ways to represent words as numbers:

1. Assign Simple Numbers

Give each category a unique number.
Example: Color → Red:0, Blue:1, Green:2

2. Use Separate Columns for Each Category

Make a column for each category, mark it as 1 if it’s present, 0 otherwise.

Example for Color:
Red	  Blue	Green
1	   0   	0
0	   1	0
0      0	1

Quick Reminder:
You have already learned these two techniques in ML for representing categories numerically (label encoding and one-hot encoding), 
here we will see them in action with tabular data. Keep this in mind as we go!

######################

Tabular MCQ
You are preparing a dataset for a machine learning model.
The dataset contains the following features:

Name	Age	Color	Country
Alice	25	Red	India
Bob	30	Blue	USA
Carol	22	Green	Japan
The model throws an error saying: "Input contains non-numeric values."
To fix this, you must convert the categorical features (Color, Country) into numbers.

Question
Which of the following is the MOST appropriate way to convert the features Color and Country into numeric form so that the model can process the data?
############################
ANSWER = Create separate columns for each category using 0s and 1s.
Red | Blue | Green -> [1,0,0], [0,1,0], [0,0,1]

Machine learning models require numeric input.  
For categories like Color and Country (no natural order), one-hot encoding is the safest method:

- It avoids creating a fake order between categories  
- It treats each category independently  
- It works well for most ML models

#################################333

Converting a Small Table to Numbers
Imagine you have a small dataset showing colors and some values:

Color	Value
Red	10
Blue	20
Green	30
The Value column is numeric → the network can use it directly.
The Color column is made of words → the network cannot process it directly.
Goal: Represent the Color column in a numeric form so the neural network can understand it.

Step 1: Assign Simple Numbers (Label Encoding)

We give each color a unique number:
Red → 0
Blue → 1
Green → 2
This converts the Color column into a numeric column called Color_Label.

Result:

Color	Value	Color_Label
Red	10	0
Blue	20	1
Green	30	2
Step 2: Use Separate Columns for Each Category (One-Hot Encoding)

Create a separate column for each color.
Mark 1 if the row has that color, 0 otherwise.
Result:

Blue	Green	Red
0	0	1
1	0	0
0	1	0
Key Takeaways:

Neural networks cannot work directly with words.
We can represent categories numerically in two ways:
Assigning a unique number to each category (label encoding)
Creating separate columns for each category (one-hot encoding)
Use the method depending on the type of data and model requirements.
Expected Output

Original Data:
   Color  Value
0    Red     10
1   Blue     20
2  Green     30

After Label Encoding:
   Color  Value  Color_Label
0    Red     10            0
1   Blue     20            1
2  Green     30            2

One-Hot Encoding:
   Blue  Green  Red
0     0      0    1
1     1      0    0
2     0      1    0

############################  CODE HERE #############

import pandas as pd

# Sample table
data = pd.DataFrame({
    "Color": ["Red", "Blue", "Green"],
    "Value": [10, 20, 30]
})

print("Original Data:")
print(data)

# Label encoding
label_map = {"Red": 0, "Blue": 1, "Green": 2}
data["Color_Label"] = data["Color"].map(label_map)

print("\nAfter Label Encoding:")
print(data)

# One-hot encoding
one_hot = pd.get_dummies(data["Color"], dtype=int)  

print("\nOne-Hot Encoding:")
print(one_hot)

#################################

Convert Categorical Data to Numbers
We have a small dataset showing different fruits and their quantities:

Fruit	Quantity
Apple	10
Mango	15
Banana	7
Apple	12
The Quantity column is numeric → the network can use it directly.
The Fruit column is made of words → the network cannot process it directly.
Goal: Represent the Fruit column in numeric form so a neural network can work with it.

Task

Choose an approach to convert the Fruit column into numbers:

Assign simple numbers (label encoding)
Use separate columns for each fruit (one-hot encoding)
Convert the table into numeric form.

Expected Output

Original Data:
    Fruit  Quantity
0   Apple        10
1   Mango        15
2  Banana         7
3   Apple        12

After Label Encoding:
    Fruit  Quantity  Fruit_Label
0   Apple        10            0
1   Mango        15            1
2  Banana         7            2
3   Apple        12            0

One-Hot Encoding:
   Apple  Banana  Mango
0      1       0      0
1      0       0      1
2      0       1      0
3      1       0      0

##################################    CODE HEREE #########

import pandas as pd

# Original table
data = pd.DataFrame({
    "Fruit": ["Apple", "Mango", "Banana", "Apple"],
    "Quantity": [10, 15, 7, 12]
})

print("Original Data:")
print(data)  # Fill in: print the original DataFrame

# ----- Label Encoding -----
label_map = {"Apple": 0, "Mango": 1, "Banana": 2}
data["Fruit_Label"] = data["Fruit"].map(label_map)  
# Fill in: access the "Fruit" column and apply the label mapping

print("\nAfter Label Encoding:")
print(data)  # Fill in: print the DataFrame after label encoding

# ----- One-Hot Encoding -----
one_hot = pd.get_dummies(data["Fruit"], dtype=int)  
# Fill in: use a pandas function to create one-hot encoded columns for "Fruit"

print("\nOne-Hot Encoding:")
print(one_hot)  # Fill in: print the one-hot encoded DataFrame

#################################33

